All of the programs are in the directory \src. you can use:
python main.py
to run the program.

The program built an environment named env. And then, the program use three kinds of search algorithm to search the path from start point to the goal point, and print the length of the path and the nodes in the path.